一个基于 Java（业务逻辑与路由）和 Python（AI模型推理）的Web图像处理系统。支持图像超分辨率（×2/×4）、色彩增强与智能降噪。所有用户数据（如处理历史）均保存在浏览器本地（IndexedDB），无需后端数据库。



用户浏览器 (Frontend)

     │

     ├─1. HTTP 请求/页面交互 (端口 8080)

     │

     ▼

┌─────────────────┐         3. 内部调用       ┌─────────────────┐

│   Java后端服务  │ ────────────────────────▶ │   Python AI服务  │

│  (Spring Boot)  │                           │   (FastAPI)     │

│   (端口:8080)   │ ◀──────────────────────── │   (端口:8000)    │

│                 │        4. 返回处理结果     │                 │

└─────────────────┘                           └─────────────────┘

         │                                          │

         │ 2. 返回HTML/静态资源                     │ 5. 加载并使用

         ▼                                          ▼  ONNX模型

┌───────────────────────────────────┐      ┌─────────────────┐

│      浏览器本地存储 (IndexedDB)     │      │  模型文件目录    │

│                                   │      │   (\*.onnx)      │

│ - 图像处理历史记录                │      └─────────────────┘

│ - 用户偏好设置                    │

│ - 临时数据                        │

└───────────────────────────────────┘



核心特点：这是一个无后端数据库的设计。用户产生的所有数据都保存在其自己的浏览器本地，完全私有。后端（Java/Python）仅提供API和计算服务，不存储任何用户数据。





\## 📁 项目结构详情



\### Java后端 (Spring Boot)
src/

├── main/

│ ├── java/

│ │ └── com/ganwork/

│ │ ├── config/ # 配置类

│ │ │ ├── AppProperties.java # 应用属性配置

│ │ │ ├── ModelConfig.java # 模型配置

│ │ │ ├── RestTemplateConfig.java # HTTP客户端配置

│ │ │ └── WebConfig.java # Web配置

│ │ │

│ │ ├── controller/ # 控制器层

│ │ │ ├── ChromeDevToolsController.java # 开发者工具控制器

│ │ │ ├── FileDownloadController.java # 文件下载控制器

│ │ │ ├── ImageController.java # 图像处理控制器

│ │ │ ├── ProcessController.java # 处理流程控制器

│ │ │ └── ViewController.java # 视图控制器

│ │ │

│ │ ├── exception/ # 异常处理

│ │ │ ├── ApiCallException.java # API调用异常

│ │ │ ├── ModelType.java # 模型类型枚举

│ │ │ ├── ResourceLoadException.java # 资源加载异常

│ │ │ └── ResourceNotFoundException.java # 资源未找到异常

│ │ │

│ │ ├── service/ # 服务层

│ │ │ ├── ApiProcessingService.java # API处理服务

│ │ │ ├── GlobalExceptionHandler.java # 全局异常处理器

│ │ │ ├── ImageProcessingService.java # 图像处理服务接口

│ │ │ ├── ImageProcessingServiceImpl.java # 图像处理服务实现

│ │ │ ├── ImageProcessingStrategy.java # 图像处理策略接口

│ │ │ ├── ModelTypeFactoryBean.java # 模型类型工厂Bean

│ │ │ └── ProcessingStrategyFactory.java # 处理策略工厂

│ │ │

│ │ └── util/ # 工具类

│ │ ├── ApiResponse.java # API响应封装

│ │ ├── FileStorageUtil.java # 文件存储工具

│ │ ├── ImageProcessingResponse.java # 图像处理响应

│ │ ├── ImageValidator.java # 图像验证器

│ │ └── JsonPathExtractor.java # JSON路径提取器

│ │

│ ├── resources/

│ │ ├── static/ # 静态资源

│ │ │ └── index.html # 前端主页面

│ │ ├── templates/ # 模板文件

│ │ ├── application.properties # Spring配置(主)

│ │ └── application.yml # Spring配置(YAML格式)

│ │

│ └── GanworkApplication.java # Spring Boot主启动类

│

├── test/ # 测试目录

└── target/ # 编译输出目录





\### Python后端 (FastAPI)
FastAPI Server/

├── download/ # 下载文件目录

├── models/ # ONNX模型文件目录

│ ├── color\_enhance.onnx # 色彩增强模型

│ ├── denoise.onnx # 降噪模型

│ ├── super\_res\_2x.onnx # 2倍超分辨率模型

│ ├── super\_res\_4x.onnx # 4倍超分辨率模型

│ └── super\_res\_manga\_4x.onnx # 漫画风格4倍超分辨率模型

│

├── uploads/ # 用户上传文件目录

├── utils/ # 工具模块

│ ├── image\_processing.py # 图像处理工具

│ └── model\_loader.py # 模型加载工具

│

├── .env # 环境变量配置

├── app.py # FastAPI主应用文件

├── config.py # 应用配置

├── model\_config.py # 模型配置

├── requirements.txt # Python依赖列表

└── schemas.py # Pydantic数据模型





\## 🚀 快速开始



\### 环境要求

Java: JDK 17 或更高版本

Python: Python 3.8 或更高版本

Maven: 3.6.x 或更高版本 (用于Java项目构建)

Git: 版本控制系统



1. 克隆项目

bash

git clone https://github.com/your-username/image-enhancement.git

cd image-enhancement

2. 启动Python FastAPI服务

# 进入Python服务目录

cd "FastAPI Server"

# 安装Python依赖

pip install -r requirements.txt

# 启动FastAPI服务（默认端口8000）

python app.py

3. 启动Java Spring Boot服务

直接运行主类

项目主类: com.ganwork.GanworkApplication

服务启动后，访问 http://localhost:8080 使用图像处理系统。



4. 使用系统

打开浏览器访问 http://localhost:8080

点击"选择文件"按钮上传图像（支持JPG、PNG格式）

选择处理功能：超分辨率、色彩增强、降噪

点击"处理"按钮，等待AI模型处理

查看处理结果，结果图像会自动保存到浏览器本地



贡献指南

欢迎贡献代码！请遵循以下步骤：

Fork项目并克隆到本地

创建功能分支: git checkout -b feature/your-feature

提交更改: git commit -m 'Add some feature'

推送到分支: git push origin feature/your-feature

创建Pull Request

代码规范

Java代码: 遵循Google Java Style Guide

Python代码: 遵循PEP 8规范

提交信息: 使用约定式提交格式

测试: 为新功能添加相应测试用例

